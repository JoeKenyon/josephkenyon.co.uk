{
  "name": "joe_site",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "rm -rf _site && npx @11ty/eleventy --serve",
    "build": "rm -rf _site && npx @11ty/eleventy",
    "deploy": "npm run build && wrangler --commit-dirty=true pages deploy _site",
    "sync-music": "node scripts/fetch-music.js",
    "sync-art": "node scripts/fetch-art.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^3.1.5",
    "@11ty/eleventy-img": "^6.0.4",
    "@11ty/eleventy-navigation": "^1.0.5",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.2",
    "jsdom": "^29.1.1",
    "prettier": "^3.8.3",
    "puppeteer": "^25.0.2"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
