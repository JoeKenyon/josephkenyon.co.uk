{
  "name": "joe_site",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "rm -rf _site && npx @11ty/eleventy --serve",
    "deploy": "astro build && wrangler pages deploy _site",
    "sync-music": "node scripts/fetch-music.js",
    "sync-art": "node scripts/fetch-art.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^3.1.5",
    "@11ty/eleventy-img": "^6.0.4",
    "@11ty/eleventy-navigation": "^1.0.5"
  },

  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
